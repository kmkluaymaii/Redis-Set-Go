# CLI service simulates user interactions (image upload, query)
# Flow:
# 1. User uploads an image → create "image.submitted" event
# 2. User submits a query → create "query.submitted" event

def handle_image(image):
    # Create image event 
    pass

def handle_query(query):
    # Create query event
    pass